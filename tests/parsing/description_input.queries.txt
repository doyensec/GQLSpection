query {
    # A user:
    # - id: ID of the user
    # - key: Key of the user
    user(id: ID = 0, key: String) {
        # Name of the user:
        # - abbreviated: Abbreviated or full name
        name(abbreviated: Boolean = false)
    }
}
